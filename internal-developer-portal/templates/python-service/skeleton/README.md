# ${{ values.name }}

Generated by the internal developer portal golden path. Ownership and system metadata are stored in `catalog-info.yaml`; the Kubernetes manifest starts from hardened runtime defaults.
