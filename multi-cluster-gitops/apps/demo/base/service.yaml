apiVersion: v1
kind: Service
metadata:
  name: payments-demo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: payments-demo
  ports:
    - name: http
      port: 80
      targetPort: 8080
