## What changed

<!-- Keep this short: what changed and why. -->

## Validation

- [ ] Project-specific tests pass
- [ ] `python tools/repo_audit.py` passes
- [ ] No credentials, state files or generated secrets are committed
- [ ] Runtime/container versions are pinned where the project relies on deterministic behavior
- [ ] README/runbook changed if operational behavior changed
