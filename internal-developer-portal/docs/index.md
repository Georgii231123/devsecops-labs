# Engineering Portal

This lab models the contract behind an internal developer portal: discoverable ownership, software catalog relationships, reusable service scaffolding and policy-driven golden paths.

The portal intentionally treats the catalog as an engineering control plane rather than a directory of links. Every production component must have a group owner, system relationship, documentation annotation and repository annotation.
