# Security Policy

This repository is a security training and portfolio lab. The manifest in `k8s/vulnerable/` is intentionally insecure and must never be deployed to a real environment.

If you identify a weakness in the hardened example or a bypass in `policy/kubernetes.rego`, open a private security report through GitHub when available rather than publishing exploit details in an issue.
