services:
  app:
    image: {{ app_image }}
    restart: unless-stopped
    ports:
      - "127.0.0.1:{{ app_listen_port }}:{{ app_container_port }}"
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    tmpfs:
      - /tmp
